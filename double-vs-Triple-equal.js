// double equal(==) just check the value / or check value and type and convert different type value in same type to compare them. it's compare two type value
// in double equal(==) true = 1, false = 0
// triple equal(===) check value and type , it's can't compare two type value
const first = 0;
const second = false;
if(first == second){
    console.log("condition is true");
}
else{
    console.log("condition is false");
}